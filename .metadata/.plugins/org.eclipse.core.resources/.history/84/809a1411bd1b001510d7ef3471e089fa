var OAIPMHHeader = require('./OAIPMHHeader.js').OAIPMHHeader;

function OAIPMHRecord(record) {
	this.record = record;
}

OAIPMHRecord.prototype.header = function() {
	return new OAIPMHHeader(this.record.header[0]);
};

export.OAIPMHRecord = OAIPMHRecord;
